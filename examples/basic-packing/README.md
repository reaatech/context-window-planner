# basic-packing

Minimal end-to-end example: builds a planner, adds a handful of items, and
prints the packing decision.

## Run

```bash
pnpm install
pnpm --filter @reaatech/context-window-planner build
pnpm --filter @example/basic-packing start
```
