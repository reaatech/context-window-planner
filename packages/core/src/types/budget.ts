/**
 * Re-export from token-budget.ts for API alignment with ARCHITECTURE.md.
 *
 * The canonical implementation lives in token-budget.ts.
 * This alias file ensures `types/budget` resolves to the same module.
 *
 * @module
 */

export { TokenBudget } from './token-budget.js';
